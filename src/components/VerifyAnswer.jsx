/* eslint-disable react/prop-types */

export const VerifyAnswer = ({
  option,
  //   correctOption,
  //   selectedAnswer,
  className,
}) => {
  return <span className={className}>{option}</span>;
};
